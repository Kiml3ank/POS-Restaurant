# ---------------------------------------------------------------------------
# Dev database — PostgreSQL 17 ที่รันด้วย docker-compose (ดู CLAUDE.md หัวข้อ 2.1)
# สั่ง `npm run db:up` ก่อน แล้วค่อย `npx prisma migrate dev`
# ---------------------------------------------------------------------------
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"

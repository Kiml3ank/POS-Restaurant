/**
 * ชื่อ cookie ของภาษา — อยู่ไฟล์เดียวโดด ๆ
 *
 * เหตุผลเดียวกับ lib/table-session-cookie.ts: ไฟล์ที่ห้าม import อะไรที่มี
 * `import "server-only"` (เช่น proxy.ts) ต้องเรียกชื่อนี้ได้ และไฟล์ที่ขึ้นต้น
 * ด้วย "use server" ก็ export ค่าคงที่ออกมาไม่ได้เลย จึงเก็บไว้ที่นี่ที่เดียว
 */
export const LOCALE_COOKIE = "pos_locale";

/**
 * หนึ่งปี — ภาษาของเครื่องไม่ควรหลุดกลับเป็นค่าตั้งต้นเพราะปิดเบราว์เซอร์
 * (เครื่อง POS ถูกปิดทุกคืน ถ้าอายุสั้นกว่านี้ครัวต้องมากดใหม่ทุกเช้า)
 */
export const LOCALE_COOKIE_MAX_AGE = 60 * 60 * 24 * 365;
